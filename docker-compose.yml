services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eprint-app
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "9000"
    volumes:
      - storage_data:/var/www/storage
    # depends_on:
    #   - db

  nginx:
    image: nginx:latest
    container_name: eprint-nginx
    restart: unless-stopped
    depends_on:
      - app
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www
      - storage_data:/var/www/storage
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

  # mysql:
  #   image: mysql:8.0
  #   container_name: eprint-db
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_DATABASE: news
  #     MYSQL_ROOT_PASSWORD: root
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql


volumes:
  storage_data:
  # mysql_data: